{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "presentation-windows",
  "description": "Presentation windows can receive backend state events but cannot forge them",
  "windows": ["main", "banner"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
